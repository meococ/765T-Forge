# E02 — Capability discovery

## Call
`forge_system_capabilities`

## Expect
- `Ok=true`
- `Data.devices` is a non-empty array (or explicit empty with reason)
- `Data.pageSetups` and `Data.layouts` present
